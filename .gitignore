.venv/
__pycache__/
*.pyc

# `main` holds code only. Everything below is regenerated by run_pipeline.py and
# published by GitHub Actions to the `site` branch (dashboard + CSVs + report).
data/
site/
