.venv/
__pycache__/
*.pyc

# scraped inputs (hundreds of MB, fully regenerable with run_pipeline.py)
data/raw/

# built database (750 MB); the CSVs and FINDINGS.md are the deliverables and stay tracked
data/out/*.db
